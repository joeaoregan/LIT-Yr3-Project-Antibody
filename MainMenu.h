#ifndef MAINMENU_H
#define MAINMENU_H

#include "Game.h"

class MainMenu {
public:
	//bool init();
	void update();
	void render();
	void close();

	bool init();
	bool loadMedia();

private:

};

#endif
